import { Action, MapState, IStore, IReducer } from '@re-store/types';
export declare class Reducer<StateType, ST = MapState<StateType>> implements IReducer<StateType, ST> {
    name: string;
    protected store: IStore;
    protected defaultState: ST;
    private reducerMethods;
    constructor(store: IStore, defaultState?: ST);
    register(key: string, fn: (state: ST, payload: any) => ST): void;
    reduce(previousState: ST, action: Action): ST;
}
